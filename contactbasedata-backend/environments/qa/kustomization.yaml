bases:
  - ../../base
patches:
  #- contact-be-configmap.yaml
  - contactbackend.yaml
configMapGenerator:
  - name: contact-be-config
    namespace: default
    #behavior: replace
    files:
      - configs/application.yml